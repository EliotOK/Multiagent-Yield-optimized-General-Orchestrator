interface:
  display_name: "MYGO — Multi-Agent Orchestrator"
  short_description: "Current-model scientific multi-agent workflow with configurable workers"
  default_prompt: "Use $research-multiagent-orchestrator. Resolve the active composer model through the project model map, then route and supervise the scientific coding task with that current model as the sole final authority."
