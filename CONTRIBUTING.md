# Contributing

Keep the bundled skill focused on the planner-worker-reviewer workflow. Put
repository documentation, examples, and CI outside the skill directory so they do
not add runtime context.

Before opening a pull request, run the PowerShell validation and installation smoke
tests. Do not add credentials, personal paths, raw data, or provider-specific
assumptions without an explicit compatibility note.
